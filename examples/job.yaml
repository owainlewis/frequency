image: golang
workspace: /go/src/github.com/owainlewis/kcd
env:
  - name: SECRET_USERNAME
    valueFrom:
      secretKeyRef:
        name: mysecret
        key: username
command:
  cmd: ./ci/build.sh
  args: []
